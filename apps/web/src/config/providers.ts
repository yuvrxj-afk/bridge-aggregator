// Providers visible/executable in UI.
// Keep this list strictly aligned to receipt-confirmed providers in STATUS.md.
export const CONFIRMED_PROVIDERS = new Set([
  "across",
  "cctp",
]);

export const VISIBLE_PROVIDERS = CONFIRMED_PROVIDERS;
